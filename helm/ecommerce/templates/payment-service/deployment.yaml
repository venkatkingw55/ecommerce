apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: {{ .Values.namespace }}
  labels:
    app: payment-service
    {{- include "ecommerce.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.paymentService.replicaCount }}
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: {{ include "ecommerce.image" (dict "registry" $.Values.imageRegistry "repository" .Values.paymentService.image.repository "tag" .Values.paymentService.image.tag) }}
          imagePullPolicy: {{ .Values.paymentService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.paymentService.service.port }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: {{ include "ecommerce.databaseUrl" (dict "user" $.Values.postgres.user "passwordEnvVar" "POSTGRES_PASSWORD" "port" $.Values.postgres.service.port "database" .Values.paymentService.database) | quote }}
            - name: CART_SERVICE_URL
              value: "http://cart-service:{{ .Values.cartService.service.port }}"
            - name: PRODUCT_SERVICE_URL
              value: "http://product-service:{{ .Values.productService.service.port }}"
          resources:
            {{- toYaml .Values.paymentService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.paymentService.probes.liveness.path }}
              port: {{ .Values.paymentService.service.port }}
            initialDelaySeconds: {{ .Values.paymentService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.paymentService.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.paymentService.probes.readiness.path }}
              port: {{ .Values.paymentService.service.port }}
            initialDelaySeconds: {{ .Values.paymentService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.paymentService.probes.readiness.periodSeconds }}
